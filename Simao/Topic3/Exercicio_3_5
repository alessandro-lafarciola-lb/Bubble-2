a)   int n = 0;
while( n < 10 ) {
//////n++;
    System.out.print( n );
}

b)   int b = 0;///should have n instead of b

while( n < 10 );{// semi colon is not needed

    System.out.print( n );

    n++;
    //////curly braces
c)    int c = 0, y = 0, x = 1;

while( n = 0 ){///should have == and n wasn't declared

    y = y + x;

    x++;

    if(x > 5) n = 1;

}
